How the Decision Tree Works

The program starts at the root node, which contains a question.

It asks the user for an answer (yes/no).

Based on the answer, the program moves to the corresponding child node.

If a leaf node is reached, the program outputs the final decision.

The process continues recursively until a decision is made.
